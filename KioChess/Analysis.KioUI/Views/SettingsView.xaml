<UserControl x:Class="Analysis.KioUI.Views.SettingsView"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:prism="http://prismlibrary.com/"
prism:ViewModelLocator.AutoWireViewModel="True"
Background="{DynamicResource SurfaceBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  Background="{DynamicResource SurfaceBrush}">
        <StackPanel Margin="32 24" MaxWidth="600">

            <!-- Engine -->
            <TextBlock Text="ENGINE" Style="{DynamicResource SubHeadingStyle}" Margin="0 0 0 8"/>
            <Border Style="{DynamicResource CardStyle}" Margin="0 0 0 20">
                <StackPanel>
                    <TextBlock Text="Stockfish Executable Path"
                               Style="{DynamicResource MutedTextStyle}" Margin="0 0 0 6"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Style="{DynamicResource ThemedTextBoxStyle}"
                                 Text="{Binding StockfishPath, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0 0 8 0"/>
                        <Button Grid.Column="1"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Content="Browse..."
                                Command="{Binding BrowseStockfishCommand}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Appearance -->
            <TextBlock Text="APPEARANCE" Style="{DynamicResource SubHeadingStyle}" Margin="0 0 0 8"/>
            <Border Style="{DynamicResource CardStyle}" Margin="0 0 0 20">
                <StackPanel>
                    <TextBlock Text="Theme"
                               Style="{DynamicResource MutedTextStyle}" Margin="0 0 0 6"/>
                    <ComboBox Style="{DynamicResource ThemedComboBoxStyle}"
                              ItemsSource="{Binding Themes}"
                              SelectedItem="{Binding SelectedTheme}"
                              DisplayMemberPath="DisplayName"/>

                    <!-- Live colour swatch strip -->
                    <ItemsControl ItemsSource="{Binding Themes}" Margin="0 12 0 12">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="28" Height="28" CornerRadius="14"
                                        Margin="4" ToolTip="{Binding DisplayName}"
                                        Cursor="Hand" BorderThickness="2"
                                        BorderBrush="Transparent">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                      Command="{Binding DataContext.SelectThemeCommand,
                                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      CommandParameter="{Binding}"/>
                                    </Border.InputBindings>
                                    <Border Background="{DynamicResource AccentBrush}" CornerRadius="12"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Piece Set"
                               Style="{DynamicResource MutedTextStyle}" Margin="0 0 0 6"/>
                    <ComboBox Style="{DynamicResource ThemedComboBoxStyle}"
                              ItemsSource="{Binding PieceSets}"
                              SelectedItem="{Binding SelectedPieceSet}"
                              DisplayMemberPath="DisplayName"/>
                </StackPanel>
            </Border>

            <!-- Sound -->
            <TextBlock Text="SOUND" Style="{DynamicResource SubHeadingStyle}" Margin="0 0 0 8"/>
            <Border Style="{DynamicResource CardStyle}" Margin="0 0 0 20">
                <StackPanel>
                    <CheckBox Style="{DynamicResource ThemedCheckBoxStyle}"
                              Content="Enable sounds"
                              IsChecked="{Binding SoundEnabled}"
                              Margin="0 0 0 10"/>
                    <TextBlock Text="Volume"
                               Style="{DynamicResource MutedTextStyle}" Margin="0 0 0 6"/>
                    <Slider Style="{DynamicResource ThemedSliderStyle}"
                            Minimum="0" Maximum="1"
                            Value="{Binding SoundVolume}"
                            IsEnabled="{Binding SoundEnabled}"
                            TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                </StackPanel>
            </Border>

            <!-- Animation -->
            <TextBlock Text="ANIMATION" Style="{DynamicResource SubHeadingStyle}" Margin="0 0 0 8"/>
            <Border Style="{DynamicResource CardStyle}" Margin="0 0 0 20">
                <StackPanel>
                    <TextBlock Text="Animation Speed"
                               Style="{DynamicResource MutedTextStyle}" Margin="0 0 0 6"/>
                    <ComboBox Style="{DynamicResource ThemedComboBoxStyle}"
                              ItemsSource="{Binding AnimationSpeeds}"
                              SelectedItem="{Binding AnimationSpeed}"/>
                </StackPanel>
            </Border>

            <!-- Save -->
            <Button Style="{DynamicResource PrimaryButtonStyle}"
                    Content="Save Settings"
                    Command="{Binding SaveCommand}"
                    HorizontalAlignment="Left"
                    Margin="0 8 0 0"/>

            <!-- Saved confirmation -->
            <TextBlock Text="Settings saved"
                       Style="{DynamicResource MutedTextStyle}"
                       Margin="0 8 0 0"
                       Visibility="{Binding SavedNoticeVisibility}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
